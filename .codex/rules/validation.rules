# Trusted-repository template validation.

prefix_rule(pattern = ["python3", "validate_templates.py"], decision = "allow", justification = "Render and validate every supported bootstrapper configuration.")
